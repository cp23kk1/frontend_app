import { BackGroundWrapper } from './style';
import { TBackgroundGamplay } from './type';

const BackGroundGameplay = ({ children }: TBackgroundGamplay) => {
  return <BackGroundWrapper>{children}</BackGroundWrapper>;
};

export default BackGroundGameplay;
